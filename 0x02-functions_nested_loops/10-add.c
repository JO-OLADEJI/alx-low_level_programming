#include "main.h"

/**
 * add - function prototype
 *
 * Description: adds two integers and returns the result
 *
 * Return: sum of inputs
 */
int add(int x, int y)
{
	int sum;

	sum = x + y;
	return (sum);
}
