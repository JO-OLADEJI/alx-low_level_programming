#include <stdio.h>

/**
 * main - entry function of program
 *
 * Description: prints a text and a new line onto stdout
 *
 * Return: zero (0) is existed properly
 *
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
